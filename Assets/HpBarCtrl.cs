using System.Collections; using System.Collections.Generic; using UnityEngine; using UnityEngine.UI;  public class HpBarCtrl : MonoBehaviour {      void onDamage(int damage)     {         Rect hpRect = HpBar1.RectTransForm;         hpRect.width = (hpRect.width - 10);          if (hpRect.width <= 0)         {             hpRect.width = 0;         }         HpBar1.RectTransForm = hpRect;     } }  